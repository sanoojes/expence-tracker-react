const Header = () => {
    return (
        <h1 className="font-bold text-2xl text-center p-4 bg-gray-50 rounded-lg border-2 border-gray-300">
            Expense Tracker | Vite + React
        </h1>
    );
};

export default Header;
